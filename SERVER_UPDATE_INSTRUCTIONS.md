# Инструкции для безопасного обновления на сервере

## Первое обновление (чтобы получить скрипт safe_pull_server.sh)

Выполните на сервере следующие команды:

```bash
# 1. Перейти в директорию проекта
cd ~/projects/project_send_landing_page_and_create_crm_order

# 2. Создать backup базы данных (на всякий случай)
if [ -f "./data/app.db" ]; then
    cp ./data/app.db ./data/app.db.backup.$(date +%Y%m%d_%H%M%S)
    echo "✅ Backup создан"
fi

# 3. Получить изменения из GitHub (включая скрипт safe_pull_server.sh)
sudo git pull origin main

# 4. Пересобрать Docker образ
sudo docker compose build web

# 5. Перезапустить контейнер (это НЕ удалит volumes с базой данных)
sudo docker compose up -d web

# 6. Проверить статус
sudo docker compose ps
sudo docker compose logs --tail=20 web
```

## Последующие обновления (после получения скрипта)

После первого обновления вы можете использовать скрипт:

```bash
cd ~/projects/project_send_landing_page_and_create_crm_order
chmod +x safe_pull_server.sh
./safe_pull_server.sh
```

## Важные моменты:

1. ✅ **База данных сохранится** - она находится в volume `./data:/app/data`, который НЕ удаляется при `docker compose up -d`
2. ⚠️ **НЕ используйте** `docker compose down -v` - это удалит volumes
3. 💾 **Backup создается автоматически** - скрипт создает резервную копию перед обновлением
4. 🔄 **Простое обновление** - используйте `docker compose up -d` вместо `docker compose down` + `docker compose up`

## Если возникли конфликты при git pull:

```bash
# Посмотреть конфликты
sudo git status

# Если нужно принять версию с сервера для конкретного файла:
sudo git checkout --ours <файл>

# Если нужно принять версию с GitHub:
sudo git checkout --theirs <файл>

# Затем добавить и продолжить:
sudo git add <файл>
sudo git commit -m "Resolve conflict"
```

